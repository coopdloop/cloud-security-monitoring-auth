# docker-compose.ci.yml (CI/CD specific)
version: '3.8'
services:
  app:
    environment:
      - AUTH0_DOMAIN=${AUTH0_DOMAIN}
      - AUTH0_CLIENT_ID=${AUTH0_CLIENT_ID}
    ports:
      - "3000:3000"
